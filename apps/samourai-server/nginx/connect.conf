# Connect UI Configuration
server {
    listen 8081;
    server_name _;

    root /var/www/connect;
    index index.html;

    # Connection details page
    location / {
        try_files $uri $uri/ =404;
    }
}
