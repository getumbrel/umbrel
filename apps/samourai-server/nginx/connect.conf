# Connect UI Configuration
server {
    listen 8081;
    server_name _;

    # Connection details page
    location / {
        alias /var/www/connect;
        index index.html;
        try_files $uri $uri/ =404;
    }
}
