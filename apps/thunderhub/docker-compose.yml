version: "3.7"

x-logging:
  &default-logging
  driver: journald
  options:
    tag: "umbrel-app {{.Name}}"

services:
  web:
    image: apotdevin/thunderhub:v0.12.0@sha256:6b7148313cddad9a4e46afd6a4eacd3f55f56f6af61828177b12059a64e400d7
    user: "1000:1000"
    logging: *default-logging
    restart: on-failure
    stop_grace_period: 5m
    ports:
      - "$APP_THUNDERHUB_PORT:$APP_THUNDERHUB_PORT"
    volumes:
      - ${LND_DATA_DIR}:/lnd:ro
      - ${APP_DATA_DIR}/data:/data
    environment:
      NO_VERSION_CHECK: "true"
      LOG_LEVEL: "debug"
      ACCOUNT_CONFIG_PATH: "/data/thubConfig.yaml"
    networks:
      default:
        ipv4_address: $APP_THUNDERHUB_IP
